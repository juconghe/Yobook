# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'YoBook' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for YoBook
  pod 'Alamofire', '~> 4.0'
  pod 'Kanna', '~> 2.1.0'
  pod 'SwiftyJSON'
target 'YoBookTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'YoBookUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
